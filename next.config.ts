import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // i18n is handled manually in App Router using [locale] dynamic routes
  // and middleware for locale detection
};

export default nextConfig;
